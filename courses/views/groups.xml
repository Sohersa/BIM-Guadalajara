<odoo>
    <data>
        <!-- explicit list view definition -->
        <record model="ir.ui.view" id="groups_list">
          <field name="name">groups list</field>
          <field name="model">courses.students.groups</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="partner_id"/>
              <field name="generation_id"/>
            </tree>
          </field>
        </record>

        <!-- actions opening views on models -->
       <!-- <record model="ir.actions.act_window" id="groups_action_window">
          <field name="name">Groups</field>
          <field name="res_model">courses.courses.students</field>
          <field name="view_mode">tree,form</field>
          <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No hay grupos. Agregue uno.</p>
          </field>
        </record> -->

        <menuitem name="Students" id="courses_menu_root" string="Alumnos" sequence="0"/>
    </data>
</odoo>
