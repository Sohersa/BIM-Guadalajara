<odoo>
    <data>
        <!-- explicit list view definition -->
        <record model="ir.ui.view" id="generations.list">
          <field name="name">generations list</field>
          <field name="model">courses.generation</field>
          <field name="arch" type="xml">
            <tree>
              <field name="partner_id"/>
              <field name="partner_email"/>
              <field name="partner_phone"/>
              <field name="partner_country"/>
              <field name="partner_city"/>
              <field name="partner_function"/>
              <field name="partner_title"/>
              <field name="partner_company"/>
              <field name="generation_id"/>
            </tree>
          </field>
        </record>

        <!-- actions opening views on models -->
        <record model="ir.actions.act_window" id="generations.action_window">
          <field name="name">Generations</field>
          <field name="res_model">courses.generation</field>
          <field name="view_mode">tree,form</field>
        </record>
        
        <!-- Top menu item -->

        <menuitem name="Students" id="students.menu_root"/>

        <!-- menu categories -->
        <!--<menuitem name="Students" id="courses.menu_students" parent="courses.menu_root"/>-->
        
    </data>
</odoo>    
